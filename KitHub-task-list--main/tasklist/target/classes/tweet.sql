CREATE TABLE tweets (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    content VARCHAR(255) NOT NULL,
    dateTime TIMESTAMP NOT NULL
);

CREATE TABLE IF NOT EXISTS tweets (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    content VARCHAR(255) NOT NULL,
    dateTime TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO tweets (content, dateTime) VALUES ('Sample Tweet 1', CURRENT_TIMESTAMP);
INSERT INTO tweets (content, dateTime) VALUES ('Sample Tweet 2', CURRENT_TIMESTAMP);
